<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_temperature"
            android:icon="@drawable/ic_menu_share"
            android:title="Show Temperature" />
     </group>

    <item android:title="Arduino">
        <menu>
            <item
                android:id="@+id/nav_command"
                android:icon="@drawable/ic_menu_send"
                android:title="Send Command" />
            <item
                android:id="@+id/nav_temperature_pressure"
                android:icon="@drawable/ic_menu_send"
                android:title="Show Temperature/Pressure" />
        </menu>
    </item>
    <item
        android:id="@+id/nav_manage"
        android:icon="@drawable/ic_menu_manage"
        android:title="Settings" />
</menu>
